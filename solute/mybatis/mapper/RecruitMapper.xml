<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.solugate.mapper.RecruitMapper">
    <select id="selectRecruitAll" resultType="Recruit">
        SELECT * FROM recruit ORDER BY id desc
    </select>

    <select id="selectRecruitPage" parameterType="PageForDB" resultType="RecruitForView">
        <![CDATA[
            SELECT @rownum:=@rownum-1 as number, r.*
            FROM (SELECT @rownum:=@rownum+1 as number_count, rt.*
                FROM (SELECT @rownum:=0) tmp1, (SELECT * FROM recruit) rt
                ORDER BY id desc) as r
            WHERE r.number_count >= #{numberCountMin}
                AND r.number_count <= #{numberCountMax}
                AND (@rownum:=#{lastNumber})=#{lastNumber}
        ]]>
    </select>

    <select id="selectContentTotalCount" resultType="Long">
        SELECT count(id) as total_content_count FROM recruit
    </select>

    <select id="selectLastId" resultType="Long">
        SELECT MAX(id) as last_id FROM recruit
    </select>

    <select id="selectOneRecruit" parameterType="long" resultType="Recruit">
        SELECT * FROM recruit WHERE id = #{id}
    </select>

    <select id="selectRecruitIdContentAll" parameterType="long" resultType="RecruitContent">
        SELECT * FROM recruit_content WHERE recruit_id = #{id} ORDER BY title_group_id
    </select>
</mapper>